<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>
<nav class="page-nav">
  <%= link_to t('common.actions.edit'), edit_book_path(@book) %> |
  <%= link_to t('common.actions.back'), books_path %>
  <%= button_to t('common.actions.destroy'), @book, method: :delete %>
</nav>
